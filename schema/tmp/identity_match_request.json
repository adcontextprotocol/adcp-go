{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "identity_match_request.json",
  "type": "object",
  "description": "IdentityMatchRequest is sent by the publisher to evaluate user eligibility. MUST NOT contain page context. package_ids MUST include ALL active packages for the buyer, not just those on the current page.",
  "x-go-name": "IdentityMatchRequest",
  "required": ["request_id", "user_token", "uid_type", "package_ids"],
  "properties": {
    "protocol_version": {
      "type": "string",
      "x-go-omitempty": true
    },
    "request_id": {
      "type": "string"
    },
    "user_token": {
      "type": "string"
    },
    "uid_type": {
      "$ref": "enums.json#/$defs/UIDType"
    },
    "package_ids": {
      "type": "array",
      "items": { "type": "string" },
      "x-go-name": "PackageIDs"
    },
    "consent": {
      "$ref": "#/$defs/ConsentSignals",
      "x-go-pointer": true,
      "x-go-omitempty": true
    },
    "country": {
      "type": "string",
      "description": "ISO 3166-1 alpha-2 country code. Routing directive — the router uses this to select the correct regional provider. The router MUST strip this field before forwarding to the buyer agent.",
      "x-go-omitempty": true
    }
  },
  "$defs": {
    "ConsentSignals": {
      "type": "object",
      "description": "ConsentSignals carries privacy consent signals for the identity agent.",
      "properties": {
        "gdpr": {
          "type": "boolean",
          "description": "Whether GDPR applies to this request.",
          "x-go-omitempty": true
        },
        "tcf_consent": {
          "type": "string",
          "description": "IAB TCF v2.2 consent string. Present when gdpr is true.",
          "x-go-omitempty": true
        },
        "gpp": {
          "type": "string",
          "description": "IAB Global Privacy Platform string.",
          "x-go-omitempty": true
        },
        "us_privacy": {
          "type": "string",
          "description": "US Privacy string (CCPA). Deprecated in favor of GPP but still widely used.",
          "x-go-omitempty": true
        }
      }
    }
  }
}
